function login(email, password, token) {
    // console.log(email, password)
    const passwordDB = 'sukses121';
    if(password!==passwordDB){
       console.log('sini cuy', {message : 'salah password', attemp:3})
    }
}

login('sopyan@gmail.com', 'sukses121!')